# AGENTS.md

## 项目定位

本仓库是面向零基础学习者的 CSP-J C++ 交互式视频课件。所有新增内容都要优先帮助学习者建立“题意 -> 算法 -> C++ 实现 -> 测试与复盘”的完整闭环；不要把 CSP-S、NOI 或大学算法课的难度伪装成入门必修。

## 内容规约

- 每个课件必须包含 `学习目标`、`具体内容`、`检验清单` 和一个可执行的练习建议。
- 一个课件只引入一个主要认知负担；示例从可运行的最小程序开始，再增加边界条件。
- 新术语首次出现时用中文解释，并给出 C++ 名称或语法形态；不要仅以英文术语作为解释。
- 课件中出现的工具名、命令、语法符号若对初学者可能陌生（例如 `g++`、`.cpp`、`main`），必须能点开 [`glossary/faq.json`](./glossary/faq.json) 中的基础词条，不得只把术语丢在页面上。
- 题目、代码和叙述必须区分“必会”“建议掌握”和“拓展”，不得把拓展内容写成 CSP-J 硬门槛。
- 涉及复杂度时，必须说清输入规模、可接受的量级和为什么该实现能通过。
- 不收集未成年人个人信息，不加入排行榜、聊天、公开作答或外链广告。

## 交互与可维护性

- 开发服务器、测试、校验与脚手架一律使用 Bun；不得新增 `node`、`npm`、`npx`、`yarn` 或 Node 运行脚本。常用命令为 `bun run dev`、`bun test`、`bun run scaffold:lessons` 和 `bun run build`。
- 静态站点只允许用 `bun run deploy` 发布（它内部先跑 `bun run build` 生成 `dist/`）；main 上的推送由 `.github/workflows/test-and-deploy.yml` 自动跑「`bun test` → 发布 → 线上抽检」，人工发布只是兜底。不得手工拼装 `dist/` 或发布未提交的内容，线上内容必须等于仓库的构建结果。
- 浏览器中的课件可以保持无框架静态页面，但所有本仓库的开发工具必须能由 Bun 直接运行。
- 每节课必须位于 `lessons/<小写课程-id>/` 目录，例如 `lessons/s1-01/`；目录中的 `lesson.json` 是该课的交付状态与边界记录。
- 课程数据以 `courseData` 为单一内容源。每节课必须有唯一 `id`、有效 `phase`、至少三项目标、三项内容和三项检验项。
- 学习进度只可保存在浏览器本地；存储不可用时必须明确提示，且不能让课件崩溃。
- 任何阶段切换、课件选择、筛选或进度修改都必须给出即时的可见反馈，并通过状态区域向辅助技术说明结果。
- 维持键盘可达、清晰焦点态和移动端可读布局；不能只依赖颜色表达状态。
- 音效只可由学习者主动操作后播放，必须提供开关并尊重 `prefers-reduced-motion`；奖励特效只能反馈学习操作，不能伪装成竞赛成绩或真实能力认证。
- 课程范围、功能边界或一级能力变化时，同步更新 `README.md`、`docs/course-plan.md`、`docs/feature-checklist.md` 与 `docs/roadmap.md`。

## 常见问题词条规约

- 词条的唯一来源是 `glossary/faq.json`。课件用 `data-faq="<id>"` 引用，并引入 `/glossary/faq-panel.js`。
- 每节已实现课件必须在 `lesson.json` 的 `faqTermIds` 中列出本课用到的词条；页面里的每个 `data-faq` 都必须能在词条表中找到。
- 词条只写基础回答：它是什么、为什么在本课出现、现在记住哪一句。禁止展开成编译器原理、标准全文或竞赛进阶知识。
- 可以提供 1 至 2 个官方或权威外部链接，必须使用 `https://`，并在新标签打开；链接不能代替基础解释。
- 词条载入失败时必须明确说明，且不能让课件其他互动中断。

## 动手编程与微练习规约

- 课件交互设计不得仅依赖点击“下一步/继续”按钮或被动单选一路通关；必须提供输入框让学习者亲自动手敲击键盘编写代码。
- 初期阶段（尤其是第一阶段及新语法引入阶段）严格实行**微编程**原则：不要求手写完整大段程序（不要求手打 `#include`、`int main()` 等样板脚手架），必须提供完备的只读代码脚手架，仅留出核心 1 至 5 行让孩子编写（例如补充变量定义、补齐 `cin >>` / `cout <<`、编写关键算式、补齐 `if` 条件或循环核心语句）。随着阶段递进再平滑提升编写比例。
- 编写练习要帮助孩子建立关键符号（分号 `;`、小括号 `()`、花括号 `{}`、双引号 `""`、运算符、流操作符 `>>` / `<<`）的键盘指法与语法肌肉记忆；对常见易错符号提供针对性的视觉提示与辅导。
- 代码输入与校验在浏览器前端以轻量、安全、确定的方式完成（如语法要素匹配、关键词与格式校验、Token 分析或预期结果模拟），严守“非在线判题（OJ）”的非目标底线，不向服务器提交或执行任意未受控 C++ 代码。
- 错误反馈必须正面、少儿友好且具体（例如提示“是不是漏了末尾的半角分号 `;` 呀？”、“变量名大小写检查一下哦”），严禁直接丢出晦涩难懂的英文编译器日志。
- 必须提供“查看提示”、“参考代码 / 一键对比”与“重置”等兜底能力，确保学习者卡壳时有渐进线索可寻，严禁因卡在某个字符而导致整个课件流程完全锁死。
- 输入框须使用等宽字体（`monospace`）；移动端输入框必须显式声明 `autocapitalize="none"`、`autocorrect="off"`、`spellcheck="false"`，避免移动软键盘自动大写或英文单词纠错干扰 C++ 关键字输入；必须保持键盘焦点可达与读屏无障碍反馈（`aria-live`）。

## 在线课程、视频与测试规约

- 一节课程从“规划”变为“可在线学习”前，必须同时具备本课专属的互动学习界面和在线测试入口；不能把课程目录页当作已完成的在线课程。
- 互动学习界面必须围绕本课学习目标设计可操作的学习活动、最小示例或过程演示，且必须包含动手编写代码的微练习（初期 1~5 行），杜绝纯点击流转；并提供继续学习、完成反馈和资源不可用时的明确说明。
- 视频课件由仓库外的制作流程负责。本仓库不生成、存储、转码或播放视频资产；如需关联外部交付物，只记录课程 ID、外部资产标识、章节定位和来源状态，不能伪造视频可用性。
- 在线测试只可从已通过校验的问题库随机组卷。每道题必须关联课程 ID、至少一个学习目标、难度、题型、正确答案和解析。
- 每一次组卷中的选择题数量必须严格多于非选择题；随机过程必须保留可复现的种子或等价试卷标识，同一次尝试不得重复出题。
- 作答完成后必须给出得分、逐题正误、解析和复习出口；题库为空、题目无效、网络资源不可用或提交中断时，必须明确告知并保持可恢复状态。
- 正式试题、答案与解析必须留下可追溯的校验证据（独立盲解 + 规则校验，证据写在 `docs/review/question-verification.json`，并由 `tests/question-verification.test.ts` 绑到题目内容上：题目一改，证据作废）；页面与文档不得宣称“人工审校”或“已审校”，只能写“已通过校验”。逐题人工阅读是可选的加强项，不是发布前置条件（2026-09-21 由项目负责人确认）。引入第三方题目时仍必须记录授权或来源。
- 在线测试不等同于在线判题。除非项目边界经明确修改，不执行、编译或评测学习者提交的任意 C++ 代码。

## 验收矩阵（硬性规定）

完整的业务能力验收矩阵维护在 [`docs/roadmap.md`](./docs/roadmap.md#验收矩阵业务能力覆盖矩阵)。以下规则是 MUST 级要求：

1. 每个一级功能 MUST 至少有一条 Happy Path E2E 验证。
2. 每个高风险功能 MUST 至少覆盖一条失败路径。
3. 每个涉及权限的功能 MUST 至少验证两种角色。
4. 每个会修改系统状态的操作 MUST 至少验证一次失败后的恢复或回滚。
5. 每次新增一级业务功能 MUST 同步新增对应 E2E 并更新 `docs/roadmap.md` 的验收矩阵，否则变更不完整。

当前项目没有登录和角色能力；若未来引入账号、教师端或协作功能，必须先补齐角色模型与双角色验证，不能以“仅内部使用”为由跳过。

## 最小验证

修改课程、脚手架或交互后运行：

```bash
bun test
```

修改交互后还要执行 [`tests/e2e/manual-checklist.md`](./tests/e2e/manual-checklist.md) 中覆盖的相关场景，并在提交说明中记录结果。
