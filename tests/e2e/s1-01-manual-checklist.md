# S1-01 手工端到端验收

在仓库根目录运行 `bun run dev`，打开 <http://localhost:4173/lessons/s1-01/>。如需从头开始，删除浏览器本地存储键 `csp-cpp-s1-01-progress-v1` 后刷新。

## E2E-S1-01-01 引导任务推进

1. 点击“点亮 main 入口”。
2. 在编译流程中依次点击“写入源文件”“开始编译”“运行程序”。
3. 在分号排错中先选择一个错误选项，再选择“在输出行末尾添加 ;”。

**预期：** 每一步都显示即时文字反馈；下一任务仅在前一任务完成后点亮；错误选项解释为什么不能修复；运行后终端区域显示 `Hello, CSP!`；正确完成会出现奖励提示，音效关闭时仍有完整视觉反馈。

补充：点击尚未点亮的任务或跳过“写入源文件”直接点击“开始编译”时，页面必须解释当前缺少哪一步，不能无反馈。

## E2E-S1-01-02 随机小测与解析

1. 进入“随机小测”，确认显示本次试卷标识和 3 道选择题。
2. 不作答时确认提交按钮不能提交；选择三题答案后提交。
3. 阅读每题解析和总分。
4. 点击“换一套题”。

**预期：** 单次试卷题目不重复；每题都有目标、四个选项、一个正确答案和解析；提交后显示逐题正误与得分；换题后试卷标识变化且不保留上一套答案。题库来源为本课 course-plan / 自编，并标记待人工审校。

## E2E-S1-01-05 题库异常降级

1. 在开发者工具中把该页内联脚本里的 `questionPool` 临时改成 `[]`，刷新页面后进入“随机小测”。
2. 恢复题库，再把任意一题的 `answer` 改成超出选项范围的值（例如 4 个选项时写成 `answer: 99`），刷新页面后进入“随机小测”。

**预期：** 两种情况都显示“题库暂时不可用或题目无效”的说明和“无法组卷”提示，提交按钮不可点击；任务进度、本地存储与常见问题互动继续可用，不出现白屏或伪造的分数。

## E2E-S1-01-03 恢复与降级

1. 完成任意任务或选择部分小测答案后刷新页面。
2. 回到当前任务，确认流程和答案恢复。
3. 在禁止站点存储的浏览器环境中重复一次操作。

**预期：** 正常环境能恢复任务、试卷与答案；存储不可用时页面明确说明“刷新后不会恢复进度”，页面不会卡住或伪造保存成功。

## E2E-S1-01-04 常见问题解惑

1. 在任务 1 点击带虚线的 `main`。
2. 确认侧栏出现基础解释、一句“现在记住”和至少一个外部链接。
3. 关闭后进入任务 2，点击终端或课文中的 `g++`。
4. 再点右上角“常见问题”，切换到 `.cpp 源文件`。

**预期：** 解释只用初学者能懂的话；`g++` 被说明为编译器命令，而不是要求背参数；外部链接在新标签打开。词条以小卡片出现，不挡住任务按钮；点遮罩、“关闭”或“继续学习”后可立刻继续做题。

## 自动化复验记录

上面的场景可以在真实浏览器中自动重跑一遍，不需要人工点击：

```bash
bun run e2e:s1-01
```

脚本会自行启动开发服务器（`bun scripts/dev.ts`）和无头 Chrome，结束后关掉自己启动的进程并删除临时配置目录。它需要本机装有 Chrome；若换成别的可执行文件路径，用 `CSP_E2E_CHROME` 指定。它不属于 `bun test`：断言依赖真实浏览器渲染，属于体验验收而不是单元测试。

| 复验日期 | 浏览器 | 脚本 | 结果 |
| --- | --- | --- | --- |
| 2026-09-20 | Chrome 153.0.8010.50（`--headless=new`，CDP 驱动） | `tests/e2e/s1-01-cdp.ts` | 62/62 项检查通过 |

场景与自动化覆盖的对应关系：

| 场景 | 自动化覆盖 | 仍需人工确认 |
| --- | --- | --- |
| E2E-S1-01-01 引导任务推进 | 面板解锁顺序、跳过写入/编译时的解释、四个步骤的即时文字反馈、终端 `Hello, CSP!`、完成奖励、错误选项与正确选项的排错对照 | 音效听感（脚本只检查开关存在）、键盘操作 |
| E2E-S1-01-02 随机小测与解析 | 试卷标识、3 道选择题、每题 4 个选项与检查目标、未答完不可提交、答满后提交、得分、逐题解析、正误标注、提交后锁定、换一套题后标识变化且清空作答 | 页面在窄屏下的排版 |
| E2E-S1-01-03 恢复与降级 | 刷新后恢复任务、试卷标识与解锁面板；只写入本课本地键；存储不可用时显示“不会恢复进度”的提示、互动继续可用且不自称已保存 | 真实隐私窗口中的人工复看 |
| E2E-S1-01-04 常见问题解惑 | 词条可点开、出现基础解释与“现在记住”、外链为 `https://` 且新标签打开、面板有关闭入口 | 词条卡片是否挡住任务按钮的观感 |
| E2E-S1-01-05 题库异常降级 | 空题库与答案越界两种情况都显示“题库暂时不可用或题目无效”和“无法组卷”、提交按钮不可点击、不显示伪造分数、页面其余部分仍可用；脚本临时改动题库后必须还原 | 无 |

脚本对 E2E-S1-01-05 的处理方式与人工步骤等价：临时改写页面内联脚本里的 `questionPool`，验证后立刻还原原文，并断言“临时改动已还原”通过。如果还原失败，脚本会以失败退出并保留失败证据。
